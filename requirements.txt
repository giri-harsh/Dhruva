# ANCHOR ML + backend track — fully pinned lockfile.
# Python 3.11.x ONLY (per TOOLCHAIN.md). Generated in a clean venv on 2026-09-03.
#
# Setup (Windows):
#   uv venv .venv --python 3.11
#   .venv\Scripts\activate
#   uv pip install -r requirements.txt
# or without uv:
#   py -3.11 -m venv .venv && .venv\Scripts\activate
#   pip install -r requirements.txt
#
# torch is the CPU build (no NVIDIA GPU on either dev machine). Exact install line:
#   uv pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu
# CI and both dev machines must use the CPU wheel — do not let `pip install torch`
# resolve a CUDA build (TOOLCHAIN.md §Python, "works on my machine" row).

# --- contract-pinned (must match contracts/*/requirements.txt and TOOLCHAIN.md) ---
onnx==1.22.0
onnxruntime==1.25.0
fastapi==0.141.1
pydantic==2.13.3
pydantic-core==2.46.3
uvicorn==0.46.0

# --- ML core ---
torch==2.5.1
numpy==2.4.6
pandas==3.0.5
scikit-learn==1.9.0
scipy==1.17.1
matplotlib==3.11.1

# --- test / tooling ---
pytest==9.1.1
httpx==0.28.1          # fastapi TestClient (backend/tests)

# --- transitive (pinned for byte-identical envs) ---
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.0
click==8.5.0
cloudpickle==3.1.2
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
filelock==3.32.5
flatbuffers==25.12.19
fonttools==4.64.0
fsspec==2026.7.0
h11==0.16.0
httptools==0.8.0
idna==3.19
iniconfig==2.3.0
jinja2==3.1.6
joblib==1.6.0
kiwisolver==1.5.1
markupsafe==3.0.3
ml-dtypes==0.6.0
mpmath==1.3.0
narwhals==2.25.0
networkx==3.6.1
packaging==26.3
pillow==12.3.0
pluggy==1.6.0
protobuf==7.36.1
pygments==2.21.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
python-dotenv==1.2.3
pyyaml==6.0.3
six==1.17.0
starlette==1.6.0
sympy==1.13.1
threadpoolctl==3.6.0
typing-extensions==4.16.0
typing-inspection==0.4.4
tzdata==2026.3
watchfiles==1.2.0
websockets==17.1
certifi==2026.7.22
httpcore==1.0.9
