<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        FR-22: zero network access for the entire trip, in the positioning
        path. This app requests NO INTERNET permission at all -- there is
        no code path yet that needs one (opportunistic model/map pulls are
        future work, and per the architecture they are never awaited
        mid-trip). Adding android.permission.INTERNET later is a real
        architectural decision, not a default to restore without thought.
    -->

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
